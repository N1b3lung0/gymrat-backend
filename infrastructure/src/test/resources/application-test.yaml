spring:
  docker:
    compose:
      enabled: false
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/gymrat}
    username: ${SPRING_DATASOURCE_USERNAME:gymrat}
    password: ${SPRING_DATASOURCE_PASSWORD:gymrat}
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: true

