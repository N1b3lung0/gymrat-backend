# Allow rules to pass even when no classes match the 'that()' clause.
# Needed in multi-module projects where some packages may not be on the
# test classpath as directories (they are in JARs).
archRule.failOnEmptyShould=false

